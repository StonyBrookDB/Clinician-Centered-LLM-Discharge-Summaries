# Fabricated Demo Encounter

These files are synthetic and are included only so the publication app can run locally.
Setup copies them into `eval_v2/data/raw/-1/` and encrypts the copies with your local `FILES_KEY`.
